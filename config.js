require('dotenv').config();
const config = {
    jwtSecret: process.env.SECRET_TOKEN // Replace with your actual secret key
    // You can add more configuration settings as needed
};


module.exports = config
   